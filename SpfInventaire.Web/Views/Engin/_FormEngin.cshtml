@using SpfInventaire.Core.DAL.ViewModels
@model FormEnginViewModel


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Engin</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        @if (Model.isEdit)
        {
            @Html.HiddenFor(model => model.unEngin.ID)
            @Html.HiddenFor(model => model.unEngin.DateCreation)
            @Html.HiddenFor(model => model.unEngin.DateModification)
        }

        @Html.Partial("_FormContenuEngin", Model)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                @if (Model.isEdit)
                {
                    <input type="submit" value="Enregistrer" class="btn btn-primary" />
                }
                else
                {
                    <input type="submit" value="Créer" class="btn btn-primary" />
                }
            </div>
        </div>


    </div>
}

